//Lists of enums

enum TextSize {
  small,
  medium,
  large,
}

enum OrderStatus {
  pending,
  processing,
  shipped,
  delivered,
  cancelled,
}

enum PaymentMethod {
  khalti,
  esewa,
  stripe,
  cashOnDelivery,
}
